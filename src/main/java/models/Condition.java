package models;

public enum Condition {
    /**
     * Book is in good condition, like new.
     */
    GOOD,
    /**
     * Book shows some wear, but still usable.
     */
    FAIR,
    /**
     * Book needs replacement.
     */
    NEEDS_REPLACEMENT
}
